{
  "python-03": {
    "approach": [
      {"part": 1, "prompt": "Which mappings make role updates and a user's permission union straightforward?", "concepts": ["role IDs map to permission sets", "user IDs map to assigned role IDs"], "steps": ["Validate role IDs at mutation boundaries.", "Return permission snapshots rather than internal collections."], "pitfalls": ["class-level mutable state", "returning an internal set by reference"]},
      {"part": 2, "prompt": "How can inherited permissions extend the same effective-permission query?", "concepts": ["parent links", "transitive traversal"], "steps": ["Store one parent link per role.", "Make existing permission queries include ancestors."], "pitfalls": ["checking only the immediate parent", "duplicating permission logic"]},
      {"part": 3, "prompt": "How can wildcard matching stay separate from permission collection?", "concepts": ["resource/action pairs", "exact and wildcard candidates"], "steps": ["Reuse the effective permission set.", "Compare it with the finite set of patterns that cover the request."], "pitfalls": ["treating malformed permissions as wildcards", "forgetting inherited wildcard permissions"]}
    ],
    "verify": {"commonFailures": [
      {"symptom": "Tests pass alone but fail as a suite", "cause": "Mutable role or user state is shared", "check": "Initialize every collection on each manager instance."},
      {"symptom": "Direct permissions pass but inherited checks fail", "cause": "Queries inspect only a role's own set", "check": "Trace the effective-permission query through every ancestor."}
    ]}
  },
  "react-02": {
    "approach": [
      {"part": 1, "prompt": "What state and lifecycle boundary should own streamed incidents?", "concepts": ["effect cleanup", "immutable state updates"], "steps": ["Subscribe once when the dashboard mounts.", "Derive the active newest-first view from received incidents."], "pitfalls": ["leaking a subscription on unmount", "mutating the shared seed records"]},
      {"part": 2, "prompt": "Which values are source state and which are derived display state?", "concepts": ["controlled filters", "memoized derivation"], "steps": ["Keep incidents and filter selections as separate state.", "Derive the visible list without reconnecting the feed."], "pitfalls": ["filtering incidents out of source state", "placing filter state in the subscription effect dependencies"]},
      {"part": 3, "prompt": "What snapshot is needed to roll back one optimistic containment request?", "concepts": ["optimistic updates", "per-item pending and error state"], "steps": ["Mark only the selected incident optimistically.", "On rejection, restore it and expose a retryable error."], "pitfalls": ["one global loading flag", "rollback that overwrites newer streamed state"]}
    ],
    "verify": {"commonFailures": [
      {"symptom": "Rows never appear", "cause": "The feed was not connected or subscribed", "check": "Confirm mount setup and unmount cleanup are both wired."},
      {"symptom": "Playwright cannot find a control", "cause": "A required data-testid changed", "check": "Compare the rendered attribute with the stub contract."}
    ]}
  },
  "swift-18": {
    "approach": [
      {"part": 1, "prompt": "What snapshot can carry both the stock facts and the reason an availability check failed?", "concepts": ["actor-isolated state", "typed Result outcomes", "available versus reserved units"], "steps": ["Derive one immutable snapshot from on-hand and reserved counts.", "Make adjustment validation reuse the same availability boundary."], "pitfalls": ["allowing negative available stock", "returning an opaque Bool or thrown string"]},
      {"part": 2, "prompt": "Where must idempotency lookup, availability validation, and reservation mutation occur to make overselling impossible?", "concepts": ["actor serialization", "idempotency-key replay", "single mutation boundary"], "steps": ["Check the idempotency key before validating replay arguments.", "Consume the Part 1 availability result before recording one reservation."], "pitfalls": ["checking availability outside the actor", "incrementing stock twice on key replay"]},
      {"part": 3, "prompt": "How can expiry and commit share reservation transitions while preserving all-or-nothing order behavior?", "concepts": ["injected time", "validate-then-apply", "terminal reservation states"], "steps": ["Collect eligible IDs, then expire them through release.", "Resolve and validate every commit member before applying any terminal transition."], "pitfalls": ["using the wall clock", "partially committing before a later reservation fails"]}
    ],
    "verify": {"commonFailures": [
      {"symptom": "Concurrent attempts reserve more than the SKU owns", "cause": "Availability and mutation are split across isolation boundaries", "check": "Trace both operations through one uninterrupted actor method."},
      {"symptom": "A replay changes counters or returns a new ID", "cause": "The key lookup happens after request validation or mutation", "check": "Make an existing idempotency key the first successful return path."},
      {"symptom": "A failed multi-reservation commit changes earlier reservations", "cause": "Validation and mutation are interleaved", "check": "Resolve the complete active set before closing any reservation."}
    ]}
  },
  "python-01": {
    "approach": [
      {"part": 1, "prompt": "How will boundary coordinates and missing locations be handled consistently?", "concepts": ["inclusive bounds", "missing-coordinate guard"], "steps": ["Reject incomplete locations first.", "Check both axes against their inclusive ranges."], "pitfalls": ["exclusive edge comparisons", "truthiness checks that reject zero coordinates"]},
      {"part": 2, "prompt": "How can zone lookup reuse the membership rule without changing insertion-order behavior?", "concepts": ["ordered dictionary iteration", "first match"], "steps": ["Resolve the asset safely.", "Return the first zone whose membership check succeeds."], "pitfalls": ["duplicating coordinate comparisons", "raising for an unknown asset"]},
      {"part": 3, "prompt": "Which old values must be captured before applying a location update?", "concepts": ["state transition", "three-field rule matching"], "steps": ["Preserve the old zone before updating coordinates.", "Recompute through the Part 2 lookup and evaluate rules only on a transition."], "pitfalls": ["losing the previous zone", "firing alerts when the zone is unchanged"]},
      {"part": 4, "prompt": "Where should uniqueness and referential cleanup be enforced?", "concepts": ["mutation boundaries", "stable string identifiers"], "steps": ["Validate duplicate IDs before mutation.", "When removing a zone, clear affected asset references without generating alerts."], "pitfalls": ["partially mutating before validation", "treating administrative removal as a GPS transition"]}
    ],
    "verify": {"commonFailures": [
      {"symptom": "Edge coordinates are classified outside", "cause": "Bounds use strict comparisons", "check": "Review both minimum and maximum comparisons for inclusivity."},
      {"symptom": "Unexpected alerts accumulate", "cause": "Rules run without a real zone transition", "check": "Compare old and recomputed zone IDs before matching rules."}
    ]}
  },
  "swift-10": {
    "approach": [
      {"part": 1, "prompt": "Which keyed stores and one canonical ordering rule make registration and matching predictable?", "concepts": ["typed identifiers", "deterministic multi-key sorting"], "steps": ["Validate uniqueness before inserting records.", "Centralize severity, timestamp, and ID ordering."], "pitfalls": ["using names as identity", "different ordering rules across queries"]},
      {"part": 2, "prompt": "What should manual assignment return so automatic assignment can reuse both its mutation and resulting capacity facts?", "concepts": ["typed expected failures", "rich assignment snapshot"], "steps": ["Count only assigned unresolved incidents.", "Return the updated incident, responder, and capacity snapshot together."], "pitfalls": ["counting resolved work", "returning only a Boolean or responder ID"]},
      {"part": 3, "prompt": "Where should eligibility selection end and authoritative assignment begin?", "concepts": ["strategy protocol", "composition boundary"], "steps": ["Build candidates from subscriptions and current capacity.", "Ask the strategy to choose, then delegate to manual assignment."], "pitfalls": ["letting the strategy mutate manager state", "duplicating manual validation in auto-assignment"]}
    ],
    "verify": {"commonFailures": [
      {"symptom": "Automatic assignment chooses a full or unrelated responder", "cause": "The strategy received unfiltered candidates", "check": "Inspect the candidate boundary for both subscription and available capacity."},
      {"symptom": "Manual and automatic results drift apart", "cause": "Automatic assignment mutates incidents independently", "check": "Trace the selected ID into the public manual-assignment method."}
    ]}
  },
  "swift-05": {
    "approach": [
      {"part": 1, "prompt": "How can one chronological history reduction produce each active medication snapshot?", "concepts": ["immutable events", "latest-event reduction"], "steps": ["Filter by typed patient and medication IDs.", "Sort by the comparable timestamp before selecting the latest event."], "pitfalls": ["trusting arrival order", "treating a stop event as an active zero dose"]},
      {"part": 2, "prompt": "Which patient-level counts can be expressed directly over the Part 1 history?", "concepts": ["composed reductions", "enum filtering"], "steps": ["Reuse chronological history for optional direction counts.", "Accumulate only decrease and stop directions for the summary."], "pitfalls": ["parallel event filtering logic", "including medications with no de-escalation"]},
      {"part": 3, "prompt": "How can population queries reuse patient snapshots while remaining deterministic?", "concepts": ["set uniqueness", "stable ranking"], "steps": ["Derive unique typed patient and medication IDs from events.", "Sort patient IDs and break equal counts by medication ID."], "pitfalls": ["returning duplicate patients", "depending on dictionary iteration order"]},
      {"part": 4, "prompt": "What event identity enables last-write-wins without mutating either the input collection or existing tracker?", "concepts": ["value semantics", "composite event identity"], "steps": ["Remove an event matching patient, medication, and timestamp.", "Return a new tracker containing the replacement event."], "pitfalls": ["mutating shared storage", "treating direction as part of replacement identity"]}
    ],
    "verify": {"commonFailures": [
      {"symptom": "Out-of-order inputs produce an old current dose", "cause": "Arrival order was used as chronology", "check": "Confirm every latest-event decision follows comparable timestamp ordering."},
      {"symptom": "Adding an event changes the original tracker", "cause": "Mutable reference-backed storage is shared", "check": "Verify ingestion constructs and returns a new value snapshot."}
    ]}
  },
  "swift-13": {
    "approach": [
      {"part": 1, "prompt": "How can the compiler prove every source state has an explicit transition decision?", "concepts": ["exhaustive enum switching", "typed expected failures", "immutable snapshots"], "steps": ["Keep mutable storage private to the lifecycle value.", "Decide allowed destinations with a switch over every source state before appending audit history."], "pitfalls": ["stringly typed states", "mutating state before validation", "forgetting the initial audit entry"]},
      {"part": 2, "prompt": "How can bulk advancement preserve exactly the behavior of a single transition?", "concepts": ["method composition", "partial success", "deterministic ordering"], "steps": ["Call the single-contract transition for each requested ID.", "Capture typed failures and continue processing later IDs."], "pitfalls": ["duplicating transition rules", "aborting the whole batch on one failure"]},
      {"part": 3, "prompt": "Which existing snapshots contain enough information for metrics and overdue reporting?", "concepts": ["state aggregation", "latest audit event", "whole-day arithmetic"], "steps": ["Aggregate current contract snapshots by enum case.", "Use the latest audit timestamp and exclude terminal states before sorting."], "pitfalls": ["using creation time after later transitions", "including contracts stuck exactly 30 days"]}
    ],
    "verify": {"commonFailures": [
      {"symptom": "A legal transition is unexpectedly rejected", "cause": "One branch of the lifecycle switch omits a documented destination", "check": "Walk every source enum case and compare its allowed destinations with the state diagram."},
      {"symptom": "Bulk and single transitions produce different audit trails", "cause": "Bulk logic implements a second state machine", "check": "Confirm each bulk item delegates to the single transition method."}
    ]}
  },
  "swift-15": {
    "approach": [
      {"part": 1, "prompt": "How can one directional scan express rows, columns, and both diagonal families?", "concepts": ["generic hashable players", "typed game outcomes", "direction vectors"], "steps": ["Represent occupied cells by Position.", "Return the first complete winning run, otherwise distinguish available cells from a full board."], "pitfalls": ["treating empty lines as wins", "hard-coding X and O"]},
      {"part": 2, "prompt": "Which operation should be the single source of truth for the result after a move?", "concepts": ["mutating value types", "copy independence", "method composition"], "steps": ["Validate the position before changing storage.", "After storing a mark, return the existing board result."], "pitfalls": ["reference-backed shared storage", "partially mutating on an invalid move"]},
      {"part": 3, "prompt": "How does winLength change a full-line check into a consecutive-run check?", "concepts": ["configurable dimensions", "consecutive tracking", "four canonical directions"], "steps": ["Validate size and win length together.", "Search bounded starts and stop a run when the player changes."], "pitfalls": ["counting nonconsecutive marks", "missing descending diagonals", "accepting impossible configurations"]}
    ],
    "verify": {"commonFailures": [
      {"symptom": "Changing a copied board changes the original", "cause": "Board storage is shared through a reference", "check": "Create two copies, place different marks, and inspect both positions."},
      {"symptom": "Large boards report wins across gaps", "cause": "The algorithm counts a player's marks without tracking adjacency", "check": "Trace one direction and reset the run at every different or empty cell."}
    ]}
  }
}
