// Generated by tools/build_journey_data.py; do not edit.
window.INTERVIEW_EXECUTION = window.INTERVIEW_EXECUTION || {};
window.INTERVIEW_EXECUTION["swift-23"] = {"timeBudget": {"navigation": 4, "solution": 9, "coding": 26, "verification": 6}, "navigation": {"say": "There is one rule here on three topologies, so I want to pin the rule down before I write anything.", "questions": ["Are the gap counting and the debt planning independent, or does a schedule constrain which zones may be drained?", "On a ring, do the first and last zones neighbour each other for every ring size, including two?", "In the hierarchical fleet, are a zone's only neighbours its parent and children?"], "assumptions": ["Debts are non-negative; a negative one is a typed failure naming the zone.", "Where draining and skipping clear the same debt, the plan skips, so the selection stays minimal.", "The selection is reported ascending by rack position."], "edgeCases": ["a cycle of no zones, which has exactly one schedule rather than none", "a rack of zero, one, and two zones, where the base entries are the whole answer", "a ring of two or fewer zones, where the ring rule adds nothing", "an absent hierarchical fleet, and a fleet where the root loses to its own children"], "restate": "Count advance schedules over a step set, then maximise cleared debt under a no-two-neighbours rule on a rack, on a ring, and on a tree, reporting the drained zones for the rack."}, "solution": {"pitch": "One take-or-skip recurrence stated once. The rack is where it lives; the ring is that rack run twice on a slice; the tree is the same two states folded upward instead of leftward.", "dataStructures": ["One Int array per problem: counts for Part 1, best-so-far for Part 2. Both are filled in ascending index order because every dependency points backwards.", "The rack keeps the whole array rather than two rolling scalars, because the selection is recovered by walking it in reverse and two scalars cannot answer that."], "types": ["MaintenancePlan pairs the total with the selection, so one value carries both and they cannot drift apart.", "ZoneNode is the one reference type in the problem; PlannerError makes every refusal typed, including the overflow refusal on a long cycle."], "composition": "planLinear owns the only linear table and bestLinearValue reads its total. bestRingValue calls bestLinearValue twice, on the zones without the last and without the first. bestHierarchicalValue cannot call either, and says so.", "tradeoff": "Rolling scalars would make Part 2 constant space, and would forfeit the selection entirely. Keeping the array costs O(n) memory and is what makes Part 2 answerable at all.", "trap": "The second base entry. It is the better of the first two zones, not the second zone's debt, and the difference only shows up when the first zone is the valuable one."}, "coding": {"order": ["Part 1 first: it is the same table shape with a sum instead of a max, and it warms up the base-case discipline the rest needs.", "Write planLinear with the table and the backtrack together, then bestLinearValue as a one-line projection.", "Add bestRingValue as two calls plus a max, with the small-ring case falling out of the same call.", "Finish with the post-order fold, returning both states per node from one private helper."], "commentary": ["Position zero holds the empty schedule, which is why the count is one rather than zero.", "At each zone I only ever choose between draining it and forfeiting its neighbour, or skipping it.", "I set the first two entries explicitly, because the second one is the better of the first two zones.", "Closing the rack into a ring only forbids draining both ends, so every legal plan drops the first zone or the last.", "The tree keeps the same two states the array encoded by position, folded upward instead."], "checkIn": "After Part 2, say: the plan owns the table and the value method reads it, so the total and the selection cannot disagree. Is that the split you want?"}, "verification": {"walkthroughs": ["Walk gaps of one and two by hand: the counts run 1, 1, 2, 3, 5, 8 from position zero, so a cycle of five zones admits 8 schedules.", "Walk the rack 4, 9, 3, 7: the table fills 4, 9, 9, 16, and the backtrack reads off the second and fourth zones for a total of 16.", "Walk the ring 2, 3, 2: dropping the last zone gives 3, dropping the first gives 3, so the ring clears 3 while one careless rack pass would claim 4.", "Fold the worked tree writing both states at each node: the leaf is 1 and 0, its parent is 3 and 1, the other child is 2 and 0, and the root is 4 drained against 5 intact, so the fleet clears 5."], "tests": "Run swift test --filter Problem23AvailabilityZoneMaintenancePlannerTests and read the four suites in part order; the three-zone ring and the rack of 2, 1, 1, 2 are the two fixtures that catch a wrong recurrence.", "complexity": "Part 1 is O(n times the number of gaps). Part 2 is O(n) time and O(n) space for the table the backtrack needs. Part 3 is two Part 2 passes. Part 4 visits every node once, O(n), with recursion depth equal to the fleet depth.", "moreTime": "The same table answers a minimum-cost walk by swapping the fold for a min over the same step set. A related transform is worth naming: when draining a zone forbids every zone of an adjacent value rather than an adjacent position, bucket the debts by value first and the problem becomes this rack again."}, "code": "/// One availability zone and the maintenance debt draining it would clear.\npublic struct Zone: Equatable, Sendable {\n    public let id: String\n    public let debtCleared: Int\n\n    public init(id: String, debtCleared: Int) {\n        self.id = id\n        self.debtCleared = debtCleared\n    }\n}\n\n/// A zone in a hierarchical fleet. A zone's neighbours here are its parent and\n/// its children, so the adjacency rule is the same rule the rack obeys, stated\n/// over a different topology.\npublic final class ZoneNode: Sendable {\n    public let zone: Zone\n    public let children: [ZoneNode]\n\n    public init(zone: Zone, children: [ZoneNode]) {\n        self.zone = zone\n        self.children = children\n    }\n}\n\npublic struct MaintenancePlan: Equatable, Sendable {\n    public let totalDebtCleared: Int\n    /// Ascending by rack position.\n    public let selectedZoneIDs: [String]\n\n    public init(totalDebtCleared: Int, selectedZoneIDs: [String]) {\n        self.totalDebtCleared = totalDebtCleared\n        self.selectedZoneIDs = selectedZoneIDs\n    }\n}\n\npublic enum PlannerError: Error, Equatable, Sendable {\n    case negativeZoneCount\n    case emptyGapSet\n    case nonPositiveGap(Int)\n    case negativeDebt(String)\n    case countOverflow\n    case notImplemented\n}\n\npublic struct MaintenancePlanner: Sendable {\n    /// A cycle longer than this can produce more schedules than an Int holds, so\n    /// the planner refuses rather than reporting a wrapped count.\n    private static let maximumCountableZones = 60\n\n    public init() {}\n\n    // MARK: Part 1 - Count legal advance schedules\n\n    /// How many distinct advance sequences walk the drain cursor from the start\n    /// of a cycle to exactly past its end.\n    ///\n    /// The table entry is \"schedules that end with the cursor resting on\n    /// position i\", which makes the transition a sum over the gaps that could\n    /// have produced the last advance. Position 0 has exactly one schedule: the\n    /// empty one. Duplicate gaps describe the same advance and are counted once.\n    public func scheduleCount(zoneCount: Int, allowedGaps: [Int]) throws(PlannerError) -> Int {\n        guard zoneCount >= 0 else { throw .negativeZoneCount }\n        guard !allowedGaps.isEmpty else { throw .emptyGapSet }\n        for gap in allowedGaps where gap <= 0 { throw .nonPositiveGap(gap) }\n        guard zoneCount <= Self.maximumCountableZones else { throw .countOverflow }\n\n        let gaps = Set(allowedGaps)\n        var schedules = Array(repeating: 0, count: zoneCount + 1)\n        schedules[0] = 1\n        for position in 1..<(zoneCount + 1) {\n            for gap in gaps where gap <= position {\n                schedules[position] += schedules[position - gap]\n            }\n        }\n        return schedules[zoneCount]\n    }\n\n    // MARK: Part 2 - Plan a linear rack\n\n    /// The plan is the primitive: it owns the only linear table in this file, and\n    /// the value method below is a projection of it. Two neighbours may never be\n    /// drained in the same cycle, so the choice at each position is \"drain this\n    /// zone and forfeit its predecessor\" or \"skip it and keep the best so far\".\n    ///\n    /// The backtrack reads the same table in reverse. Where draining and skipping\n    /// tie, the plan skips, which keeps the selection minimal.\n    public func planLinear(zones: [Zone]) throws(PlannerError) -> MaintenancePlan {\n        try validateDebts(zones)\n        guard !zones.isEmpty else { return MaintenancePlan(totalDebtCleared: 0, selectedZoneIDs: []) }\n\n        var best = Array(repeating: 0, count: zones.count)\n        best[0] = zones[0].debtCleared\n        if zones.count > 1 {\n            best[1] = max(zones[0].debtCleared, zones[1].debtCleared)\n        }\n        for position in 2..<max(2, zones.count) {\n            best[position] = max(best[position - 1], best[position - 2] + zones[position].debtCleared)\n        }\n\n        var selected: [String] = []\n        var position = zones.count - 1\n        while position >= 0 {\n            if position == 0 {\n                if best[0] > 0 { selected.append(zones[0].id) }\n                break\n            }\n            if best[position] == best[position - 1] {\n                position -= 1                       // a tie means this zone was skipped\n            } else {\n                selected.append(zones[position].id)\n                position -= 2                       // its neighbour is forfeited\n            }\n        }\n\n        return MaintenancePlan(totalDebtCleared: best[zones.count - 1], selectedZoneIDs: selected.reversed())\n    }\n\n    public func bestLinearValue(zones: [Zone]) throws(PlannerError) -> Int {\n        try planLinear(zones: zones).totalDebtCleared\n    }\n\n    // MARK: Part 3 - Plan a ring\n\n    /// A ring adds exactly one constraint to a rack: the first and last zones are\n    /// now neighbours, so they cannot both be drained. Every legal ring plan\n    /// therefore leaves out the first zone or the last one, which makes this two\n    /// rack problems rather than a new recurrence.\n    ///\n    /// A cycle of two or fewer zones is already a rack: no pair of them is\n    /// adjacent twice, so the linear planner answers it directly.\n    public func bestRingValue(zones: [Zone]) throws(PlannerError) -> Int {\n        guard zones.count > 2 else { return try bestLinearValue(zones: zones) }\n        let withoutLast = try bestLinearValue(zones: Array(zones.dropLast()))\n        let withoutFirst = try bestLinearValue(zones: Array(zones.dropFirst()))\n        return max(withoutLast, withoutFirst)\n    }\n\n    // MARK: Part 4 - Plan a hierarchical fleet\n\n    /// This is the one part that cannot call an earlier one: the rack table is\n    /// indexed by position, and a tree has no positions. What does carry over is\n    /// the pair of states the table encoded positionally. Where the rack asked\n    /// \"best up to here having drained this zone\" and \"best up to here without\n    /// it\", the tree asks the same two questions of every subtree and folds them\n    /// upward in post-order.\n    public func bestHierarchicalValue(root: ZoneNode?) throws(PlannerError) -> Int {\n        guard let root else { return 0 }\n        try validateTreeDebts(root)\n        let outcome = fold(root)\n        return max(outcome.drained, outcome.intact)\n    }\n\n    /// The best cleared debt for the subtree rooted at `node`, in both states.\n    /// Draining a zone forfeits every child; leaving it intact lets each child\n    /// choose its own better state.\n    private func fold(_ node: ZoneNode) -> (drained: Int, intact: Int) {\n        var drained = node.zone.debtCleared\n        var intact = 0\n        for child in node.children {\n            let childOutcome = fold(child)\n            drained += childOutcome.intact\n            intact += max(childOutcome.drained, childOutcome.intact)\n        }\n        return (drained, intact)\n    }\n\n    // MARK: Validation shared by every planning method\n\n    private func validateDebts(_ zones: [Zone]) throws(PlannerError) {\n        for zone in zones where zone.debtCleared < 0 { throw .negativeDebt(zone.id) }\n    }\n\n    private func validateTreeDebts(_ node: ZoneNode) throws(PlannerError) {\n        if node.zone.debtCleared < 0 { throw .negativeDebt(node.zone.id) }\n        for child in node.children { try validateTreeDebts(child) }\n    }\n}\n"};
