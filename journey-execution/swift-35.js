// Generated by tools/build_journey_data.py; do not edit.
window.INTERVIEW_EXECUTION = window.INTERVIEW_EXECUTION || {};
window.INTERVIEW_EXECUTION["swift-35"] = {"timeBudget": {"navigation": 4, "solution": 9, "coding": 26, "verification": 6}, "navigation": {"say": "This is a two-pointer problem with the array taken away, so the first thing I want settled is the memory constraint, because it is the only thing that rules out the obvious answer. A visited set solves every part of this in five lines, and if it is allowed I should just say so rather than write something cleverer for no reason.", "questions": ["Is the constant-memory requirement real, or is a visited set per resolution acceptable? It changes the whole approach.", "Is an id that appears only as a target a destination, and an id that appears nowhere an error? I want the difference stated.", "For the query counting from the end, does the destination itself count as one, or as zero?"], "assumptions": ["Memory per resolution is fixed: cursors and counters, nothing that grows with the chain.", "An id that appears as a target but never as an alias is a real destination; an id that appears nowhere is a typo and is refused.", "The destination counts as one when counting from the end, so an offset of one is the destination and an offset equal to the id count is the start.", "The midpoint of an even-length chain is the later of the two middles."], "edgeCases": ["an id that is already a destination, which is a chain of one and where every off-by-one shows", "an alias pointing at itself, and a loop of two, where the meeting point happens to be the entry", "a tail feeding a longer loop, which is the only shape where phase two is observable", "an empty table, and an id the table has never heard of"], "restate": "Given an alias table where each alias has at most one target, report whether a chain from an id ends or loops, resolve it to a destination and hop count or to a loop entry and length, answer two positional questions about a chain that ends, and summarise the whole table, all in memory that does not grow with chain length."}, "solution": {"pitch": "Two cursors moving at different rates over a structure that has one successor per node. The first part is the detection walk and its return type is the design decision: it hands back the meeting id rather than a yes or no, because that id is the only thing the second part needs to start from. The second part is phase two of the same algorithm plus a hop count. The third part is the same rate trick used for something completely different, which is what makes the technique feel general rather than like a trick. The fourth is the second part applied across the table.", "dataStructures": ["Two string cursors and an integer counter. Nothing per resolution grows with the chain, which is the whole constraint.", "The alias table itself, which precomputes the set of ids it knows so a typo can be told from a destination without a scan.", "A set of loop entries in the audit, because many aliases can feed one loop and the answer wants each loop once.", "A single record for the longest chain, replaced only on a strictly greater length."], "types": ["The probe is an enum with two cases carrying ids rather than an optional or a boolean, which is what stops the second part re-running the first.", "The resolution is an enum with three cases, so an unknown id is a value a caller can switch on rather than a refusal it has to catch mid-audit.", "The audit uses a small named record for the longest chain rather than a tuple, because a tuple is not equatable and this crosses a test boundary.", "The error enum separates an offset that is not positive from one that is longer than the chain, and the second carries the real length, because those are different mistakes for the caller to make."], "composition": "The chain runs one way through the whole file. The second part calls the first and does phase two only. The third part asks the second whether the chain ends rather than working that out again, which is both the composition and the correct behaviour, since a loop has no midpoint. The fourth part is the second applied to every alias. The return types are what make this possible: a probe that answered true or false, or a resolution that threw on an unknown id, would each break the link immediately below them.", "tradeoff": "Resolving the same chain repeatedly re-walks it, and a cache keyed on id would make an audit of a deep table much faster. I am not adding one because it would be the visited set arriving through the back door and it would break the per-request memory budget the resolver exists to respect. If the audit became the hot path I would build the reverse graph once and process it in bulk rather than memoise the single-id path.", "trap": "Phase two is the trap and it is a proof rather than a coding detail. After the cursors meet, the distance from the start to the loop entry equals the distance from the meeting point to the entry, and that is what licenses resetting one cursor to the start and stepping both at rate one. Someone who cannot reconstruct that argument writes a plausible variant that is right on a loop of one or two and wrong on a tail feeding a loop of three."}, "coding": {"order": ["Write the known-id check first, because three of the four parts start with it.", "Write the detection walk, checking both hops of the fast cursor before taking either.", "Decide the return type out loud before writing the signature, since that decision is what the next part lives on.", "Write the hop count and phase two next, and state the distance argument in a comment before the loop it justifies.", "Write the shared gate for the positional queries as a call into the resolution, not as a second detection walk.", "Write the two positional queries, then the audit last, which is a loop over the table and nothing else."], "commentary": ["There is no array here and no ordering, only the promise that each id has at most one successor. That is all two pointers ever actually needed.", "I check that the fast cursor's first hop exists before taking its second, otherwise the chain ends and I step into nothing.", "I am returning the meeting id rather than a yes or no. A yes would tell the next part a loop exists and nothing about where, and it would have to run this whole walk again.", "The meeting point is not the entry. The distance from the start to the entry equals the distance from the meeting point to the entry, which is why I reset one cursor to the start and step both once.", "An id that is already a destination is zero hops from itself. That is the case where an off-by-one here would first show.", "The midpoint uses the same rate difference for something completely different: when the fast cursor runs out of chain, the slow one is on the middle.", "For the query from the end I send one cursor out by the offset first, then move both together. The gap between them is the answer and nothing is stored.", "The positional queries ask the resolution whether the chain ends rather than detecting it again. A loop has no midpoint, so the gate belongs there.", "The audit walks the aliases in sorted order and replaces the longest chain only on a strictly greater length, so a tie does not depend on dictionary ordering."], "checkIn": "After the first part, say: I have made the probe return where the cursors met rather than a boolean, specifically so the next part does not have to walk the chain again. Is that the level of forward planning you want, or would you rather I kept each part's interface minimal and accepted the rework?"}, "verification": {"walkthroughs": ["Walk an alias three hops from its destination and confirm the probe reports the destination and the resolution reports three hops.", "Walk an id that is already a destination and confirm it reports zero hops, which is the shortest fixture an off-by-one fails.", "Walk a tail of two hops into a loop of three: the cursors meet on the second id of the loop, and phase two has to turn that into the first. This is the only shape where the meeting point and the entry differ.", "Walk an alias pointing at itself and confirm the loop has length one, and a loop of two and confirm it has length two.", "Walk a chain of four ids through the midpoint query and confirm it returns the third, then ask for an offset one past the chain and confirm it refuses with the real length."], "tests": "Run the resolver's suite and read the four parts in order. The tailed-loop fixture in the second suite is the one that separates the meeting point from the loop entry; the self-loop and two-id loop fixtures are there precisely because they are the shapes where the wrong answer looks right. In the third suite the four offsets from one to the chain length pin the counting convention at both ends, and the fourth suite checks that the two counts add up to the number of aliases the table holds.", "complexity": "Every walk is linear in the length of the chain and uses a fixed amount of memory: two cursors and a counter, whatever the chain does. The audit is that once per alias, so it is quadratic in the worst case where every alias sits on one long chain, which is the price of not caching and is a deliberate trade against the per-request memory budget.", "moreTime": "A hop limit as a second safeguard, which is what most real resolvers actually ship and is worth contrasting with detection. Aliases with more than one target, which stops being a chain and becomes a graph and takes the constant-memory guarantee with it. And an audit that builds the reverse graph once and resolves every alias in one pass, which is the right answer the moment the audit rather than the request is the hot path."}, "code": "public struct AliasTable: Sendable {\n    private let targets: [String: String]\n    private let known: Set<String>\n\n    public init(targets: [String: String]) {\n        self.targets = targets\n        self.known = Set(targets.keys).union(targets.values)\n    }\n\n    /// The next id in the chain, or nil when this id resolves to itself: it is\n    /// a real destination rather than another alias.\n    public func target(of id: String) -> String? {\n        targets[id]\n    }\n\n    /// Whether this id appears in the table at all, either as an alias or as\n    /// something an alias points at. An id that does not is a typo rather than\n    /// a terminal.\n    public func contains(_ id: String) -> Bool {\n        known.contains(id)\n    }\n\n    /// Every id the table redirects away from, sorted, so an audit walks the\n    /// table in a stable order.\n    public var aliasIDs: [String] {\n        targets.keys.sorted()\n    }\n}\n\npublic enum ChainProbe: Equatable, Sendable {\n    case terminates(finalID: String)\n    case loops(meetingID: String)\n}\n\npublic enum Resolution: Equatable, Sendable {\n    case terminal(id: String, hops: Int)\n    case cycle(entryID: String, length: Int)\n    case unknownAlias(String)\n}\n\npublic enum ChainError: Error, Equatable, Sendable {\n    case unknownAlias(String)\n    case cyclicChain(entryID: String)\n    case nonPositiveOffset(Int)\n    case offsetBeyondChain(offset: Int, length: Int)\n    case notImplemented\n}\n\npublic struct LongestChain: Equatable, Sendable {\n    public let startID: String\n    public let hops: Int\n\n    public init(startID: String, hops: Int) {\n        self.startID = startID\n        self.hops = hops\n    }\n}\n\npublic struct ChainAudit: Equatable, Sendable {\n    public let terminalCount: Int\n    public let cyclicCount: Int\n    public let longestChain: LongestChain?\n    public let cycleEntryIDs: [String]\n\n    public init(\n        terminalCount: Int,\n        cyclicCount: Int,\n        longestChain: LongestChain?,\n        cycleEntryIDs: [String]\n    ) {\n        self.terminalCount = terminalCount\n        self.cyclicCount = cyclicCount\n        self.longestChain = longestChain\n        self.cycleEntryIDs = cycleEntryIDs\n    }\n}\n\npublic struct ChainResolver: Sendable {\n    private let table: AliasTable\n\n    public init(table: AliasTable) {\n        self.table = table\n    }\n\n    // MARK: Part 1 - Probe a chain in constant memory\n\n    /// Walk the chain with one cursor at one hop per step and another at two,\n    /// and say what happened.\n    ///\n    /// The return carries the meeting id rather than a bare yes or no, because\n    /// the meeting point is the only thing phase two has to start from. A probe\n    /// that answered `true` would leave the next part re-running this entire\n    /// walk to recover something this one already knew.\n    public func probe(from id: String) throws(ChainError) -> ChainProbe {\n        guard table.contains(id) else { throw .unknownAlias(id) }\n\n        var slow = id\n        var fast = id\n\n        while true {\n            guard let oneHop = table.target(of: fast) else { return .terminates(finalID: fast) }\n            guard let twoHops = table.target(of: oneHop) else { return .terminates(finalID: oneHop) }\n            fast = twoHops\n            // Safe: the fast cursor got two hops out of this position, so the\n            // slow cursor, which is behind it, can certainly take one.\n            slow = table.target(of: slow) ?? slow\n            if slow == fast { return .loops(meetingID: slow) }\n        }\n    }\n\n    // MARK: Part 2 - Resolve to a terminal or a named loop\n\n    /// Where this id actually ends up: a real destination and how far away it\n    /// is, or the loop it falls into and how big that loop is.\n    ///\n    /// The probe is phase one. On a chain that terminates the work left is a\n    /// single walk that counts hops. On a chain that loops the work left is\n    /// phase two, and phase two is a proof rather than a coding detail: the\n    /// distance from the start to the loop entry equals the distance from the\n    /// meeting point to the loop entry, so resetting one cursor to the start\n    /// and stepping both one hop at a time lands them together on the entry.\n    public func resolve(from id: String) -> Resolution {\n        guard let outcome = try? probe(from: id) else { return .unknownAlias(id) }\n\n        switch outcome {\n        case .terminates(let finalID):\n            var hops = 0\n            var cursor = id\n            while cursor != finalID, let next = table.target(of: cursor) {\n                cursor = next\n                hops += 1\n            }\n            return .terminal(id: finalID, hops: hops)\n\n        case .loops(let meetingID):\n            var fromStart = id\n            var fromMeeting = meetingID\n            while fromStart != fromMeeting {\n                fromStart = table.target(of: fromStart) ?? fromStart\n                fromMeeting = table.target(of: fromMeeting) ?? fromMeeting\n            }\n            let entry = fromStart\n\n            var length = 1\n            var cursor = table.target(of: entry) ?? entry\n            while cursor != entry {\n                cursor = table.target(of: cursor) ?? cursor\n                length += 1\n            }\n            return .cycle(entryID: entry, length: length)\n        }\n    }\n\n    // MARK: Part 3 - Positional queries on a terminating chain\n\n    /// The id halfway along a chain that terminates, taking the later of the\n    /// two middles when the chain has an even number of ids.\n    ///\n    /// Two cursors again, and the rate difference is doing something completely\n    /// different from what it did in Part 1: when the fast cursor runs out of\n    /// chain the slow one is standing on the middle.\n    public func midpointID(from id: String) throws(ChainError) -> String {\n        try requireTerminating(id)\n\n        var slow = id\n        var fast: String? = id\n        while let position = fast, let oneHop = table.target(of: position) {\n            slow = table.target(of: slow) ?? slow\n            fast = table.target(of: oneHop)\n        }\n        return slow\n    }\n\n    /// The id `offset` places from the end of a chain that terminates, counting\n    /// the destination itself as one.\n    ///\n    /// A lead cursor goes `offset - 1` hops out first, then both cursors step\n    /// together until the lead reaches the destination. The gap between them is\n    /// the answer, and nothing has to be stored to find it.\n    public func id(from start: String, hopsFromEnd offset: Int) throws(ChainError) -> String {\n        guard offset > 0 else { throw .nonPositiveOffset(offset) }\n        try requireTerminating(start)\n\n        var lead = start\n        var length = 1\n        for _ in 1..<offset {\n            guard let next = table.target(of: lead) else {\n                throw .offsetBeyondChain(offset: offset, length: length)\n            }\n            lead = next\n            length += 1\n        }\n\n        var trail = start\n        while let next = table.target(of: lead) {\n            lead = next\n            trail = table.target(of: trail) ?? trail\n        }\n        return trail\n    }\n\n    // MARK: Part 4 - Audit the whole table\n\n    /// Resolve every alias in the table and summarise what came back.\n    ///\n    /// This is Part 2 applied across the table and nothing else. Where two\n    /// aliases tie on length the earlier id wins, so the audit does not depend\n    /// on dictionary ordering.\n    public func audit() -> ChainAudit {\n        var terminalCount = 0\n        var cyclicCount = 0\n        var longest: LongestChain?\n        var entries: Set<String> = []\n\n        for alias in table.aliasIDs {\n            switch resolve(from: alias) {\n            case .terminal(_, let hops):\n                terminalCount += 1\n                if hops > (longest?.hops ?? -1) {\n                    longest = LongestChain(startID: alias, hops: hops)\n                }\n            case .cycle(let entryID, _):\n                cyclicCount += 1\n                entries.insert(entryID)\n            case .unknownAlias:\n                continue\n            }\n        }\n\n        return ChainAudit(\n            terminalCount: terminalCount,\n            cyclicCount: cyclicCount,\n            longestChain: longest,\n            cycleEntryIDs: entries.sorted()\n        )\n    }\n\n    // MARK: Shared gate for the positional queries\n\n    /// The positional queries only mean anything on a chain that ends, so they\n    /// share one gate, and that gate is Part 2 rather than a second walk.\n    private func requireTerminating(_ id: String) throws(ChainError) {\n        switch resolve(from: id) {\n        case .terminal:\n            return\n        case .cycle(let entryID, _):\n            throw .cyclicChain(entryID: entryID)\n        case .unknownAlias(let missing):\n            throw .unknownAlias(missing)\n        }\n    }\n}\n"};
