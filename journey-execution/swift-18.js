// Generated by tools/build_journey_data.py; do not edit.
window.INTERVIEW_EXECUTION = window.INTERVIEW_EXECUTION || {};
window.INTERVIEW_EXECUTION["swift-18"] = {"timeBudget": {"navigation": 5, "solution": 10, "coding": 24, "verification": 6}, "navigation": {"say": "I’ll confirm the behavioral contract for Inventory Reservation Ledger before choosing storage.", "questions": ["Can I confirm idempotency identity, expiry equality, oversell races, and atomic commits?", "Which ordering guarantees are observable at the API boundary?"], "assumptions": ["The supplied domain types and typed failures are the public contract.", "Injected timestamps are authoritative and equality follows the stated boundary."], "edgeCases": ["idempotency identity", "expiry equality", "oversell races", "and atomic commits"], "restate": "Protect commerce inventory with actor-isolated stock adjustments, idempotent reservations, deterministic expiry, and atomic order commits. Later parts will compose earlier behavior."}, "solution": {"pitch": "Actor-isolated state uses dictionaries for identity, sets for uniqueness, and sorting only at deterministic output boundaries.", "dataStructures": ["Dictionaries provide identity lookup; sets enforce uniqueness.", "Arrays preserve audit/event/history order; derived views are computed by shared helpers."], "types": ["Domain structs and exhaustive enums prevent invalid stringly-typed states.", "Rich Result/error/outcome values preserve the reason later parts need."], "composition": "Part 1: What snapshot can carry both the stock facts and the reason an availability check failed? → Part 2: Where must idempotency lookup, availability validation, and reservation mutation occur to make overselling impossible? → Part 3: How can expiry and commit share reservation transitions while preserving all-or-nothing order behavior?", "tradeoff": "Prefer clear invariants and deterministic scans; add indexes behind the interface only after measuring scale.", "trap": "Spot the composition trap early: What snapshot can carry both the stock facts and the reason an availability check failed? must return or preserve enough evidence for How can expiry and commit share reservation transitions while preserving all-or-nothing order behavior?; otherwise later code would duplicate the decision."}, "coding": {"order": ["Part 1: Derive one immutable snapshot from on-hand and reserved counts.", "Part 2: Check the idempotency key before validating replay arguments.", "Part 3: Collect eligible IDs, then expire them through release."], "commentary": ["I’m establishing the canonical lookup/reduction first.", "Every mutation validates before committing, and later APIs delegate to the earlier boundary.", "Actor isolation serializes the check-and-mutate critical section; I will not await inside it."], "checkIn": "Pause after Part 1 tests: ‘Does this result preserve the evidence we need for the later parts?’"}, "verification": {"walkthroughs": ["Walk the stub example and name every state change.", "Walk the boundary cases: idempotency identity, expiry equality, oversell races, and atomic commits.", "Launch competing tasks at the same injected instant and prove the invariant."], "tests": "Run the Swift suite for swift/Tests/Problem18InventoryReservationLedgerTests/Problem18InventoryReservationLedgerTests.swift and map each ordered suite to its part.", "complexity": "State lookup is O(1) average; derived scans are O(n), with O(n log n) only where deterministic sorting is required.", "moreTime": "Add property-based invariant tests, measured indexes, persistence, and observability without changing the public contract."}, "code": "import Foundation\n\n// Problem 18: Inventory Reservation Ledger\n// Swift 6, macOS 14+ | Senior | approximately 45 minutes\n//\n// Build an actor-backed inventory ledger for a commerce checkout flow. You choose\n// the internal data structures — the public interface is what matters.\n// Store all state in instance variables initialized in init. Mutable global or\n// static state will bleed between tests and ledger instances — avoid it.\n//\n// PART 1 — Typed stock and availability  (~12 min)\n// Seed stock by SKU, adjust it with typed Result failures, and return an\n// InventorySnapshot from availability. A negative adjustment may consume only\n// available stock, never units held by reservations.\n//\n// PART 2 — Idempotent concurrent reservations  (~18 min)\n// Reserve and release stock under readable string IDs and idempotency keys.\n// reserve must consume availability's Result directly. Replaying any key returns\n// its original reservation, even if the replay's other arguments differ. Because\n// all checks and mutations occur inside this actor, concurrent calls must never\n// oversell. reservation(id:) is the lookup seam used by release.\n//\n// PART 3 — Expiry and atomic order commits  (~15 min)\n// expireReservations(at:) uses the injected instant and calls release for every\n// active reservation whose expiry is at or before that instant. commit validates\n// every reservation through reservation(id:) before changing anything, then\n// commits the whole order atomically. Centralize terminal reservation bookkeeping\n// so release, expiry, and commit never maintain parallel counter logic.\n//\n/*\n * Example\n * let ledger = InventoryReservationLedger(initialStock: [\"camera\": 2])\n * async let first = ledger.reserve(id: \"res-a\", idempotencyKey: \"key-a\", sku: \"camera\", quantity: 2, expiresAt: Date(timeIntervalSince1970: 100))\n * async let second = ledger.reserve(id: \"res-b\", idempotencyKey: \"key-b\", sku: \"camera\", quantity: 2, expiresAt: Date(timeIntervalSince1970: 100))\n * let attempts = await [first, second] // -> exactly one success\n * let replay = await ledger.reserve(id: \"ignored\", idempotencyKey: \"key-a\", sku: \"camera\", quantity: 99, expiresAt: .distantFuture) // -> same res-a when key-a won\n * let expired = await ledger.expireReservations(at: Date(timeIntervalSince1970: 100)) // -> winner released; camera available is 2\n */\n\npublic struct InventorySnapshot: Equatable, Sendable {\n    public let sku: String\n    public let onHand: Int\n    public let reserved: Int\n    public let available: Int\n\n    public init(sku: String, onHand: Int, reserved: Int, available: Int) {\n        self.sku = sku\n        self.onHand = onHand\n        self.reserved = reserved\n        self.available = available\n    }\n}\n\npublic enum InventoryError: Error, Equatable, Sendable {\n    case invalidQuantity(Int)\n    case unknownSKU(String)\n    case insufficientAvailable(sku: String, requested: Int, available: Int)\n    case duplicateReservationID(String)\n    case unknownReservation(String)\n    case reservationNotActive(String)\n    case duplicateOrderID(String)\n    case emptyOrder\n    case notImplemented\n}\n\npublic enum ReservationState: Equatable, Sendable {\n    case active\n    case released\n    case expired\n    case committed(orderID: String)\n}\n\npublic struct Reservation: Equatable, Sendable {\n    public let id: String\n    public let idempotencyKey: String\n    public let sku: String\n    public let quantity: Int\n    public let expiresAt: Date\n    public let state: ReservationState\n\n    public init(id: String, idempotencyKey: String, sku: String, quantity: Int, expiresAt: Date, state: ReservationState = .active) {\n        self.id = id\n        self.idempotencyKey = idempotencyKey\n        self.sku = sku\n        self.quantity = quantity\n        self.expiresAt = expiresAt\n        self.state = state\n    }\n}\n\npublic struct CommittedOrder: Equatable, Sendable {\n    public let id: String\n    public let reservationIDs: [String]\n\n    public init(id: String, reservationIDs: [String]) {\n        self.id = id\n        self.reservationIDs = reservationIDs\n    }\n}\n\npublic actor InventoryReservationLedger {\n    public init(initialStock: [String: Int] = [:]) {}\n\n    // MARK: Part 1 — typed stock and availability\n    public func snapshot(for sku: String) -> Result<InventorySnapshot, InventoryError> { .failure(.notImplemented) }\n    public func availability(for sku: String, quantity: Int) -> Result<InventorySnapshot, InventoryError> { .failure(.notImplemented) }\n    public func adjustStock(sku: String, by quantity: Int) -> Result<InventorySnapshot, InventoryError> { .failure(.notImplemented) }\n\n    // MARK: Part 2 — idempotent concurrent reservations\n    public func reservation(id: String) -> Result<Reservation, InventoryError> { .failure(.notImplemented) }\n    public func reserve(id: String, idempotencyKey: String, sku: String, quantity: Int, expiresAt: Date) -> Result<Reservation, InventoryError> { .failure(.notImplemented) }\n    public func release(reservationID: String) -> Result<Reservation, InventoryError> { .failure(.notImplemented) }\n\n    // MARK: Part 3 — expiry and atomic commits\n    public func expireReservations(at instant: Date) -> [Reservation] { [] }\n    public func commit(orderID: String, reservationIDs: [String]) -> Result<CommittedOrder, InventoryError> { .failure(.notImplemented) }\n}\n"};
