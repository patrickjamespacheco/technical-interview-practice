// Generated by tools/build_journey_data.py; do not edit.
window.INTERVIEW_EXECUTION = window.INTERVIEW_EXECUTION || {};
window.INTERVIEW_EXECUTION["swift-31"] = {"timeBudget": {"navigation": 4, "solution": 9, "coding": 26, "verification": 6}, "navigation": {"say": "The reward for retiring a buffer depends on what is still present, so the order matters and greedy is out. I want to establish that in the first minute, because everything I do afterwards is a consequence of it.", "questions": ["Do the two ends of the chain have neighbours, or does a buffer at an end only see one real neighbour?", "How long a chain do I have to support, since the answer decides whether an exhaustive baseline is even worth writing?", "When the run-collapse optimisation lands, does a run have to be consecutive in the original chain, or only consecutive by the time it is retired?"], "assumptions": ["A missing neighbour at either end counts as a width of one, which is why padding removes every edge case rather than hiding one.", "Widths are positive and bounded by a documented maximum, and the bound is what keeps three multiplied widths inside an integer.", "A run may be assembled: buffers become consecutive as the ones between them are retired, which is the only reading under which the last part is interesting.", "Identifiers are unique, and a repeat is a refusal rather than something to disambiguate."], "edgeCases": ["an empty chain and a chain of one, where the interval loop has no length to iterate and would otherwise trap", "a chain where the best order is not sorted by width in either direction, which kills the obvious greedy heuristic", "a chain whose stage types are all distinct, where no run can ever exceed one buffer", "a chain that is entirely one stage type, which is the largest a single run can be"], "restate": "Given a chain of buffers, choose the order to retire them so the total saving is largest, where retiring a buffer earns its width times the widths of the neighbours still present; report that total, report an order that achieves it, and then solve the variant where same-type buffers retire together."}, "solution": {"pitch": "The state is a stretch of the chain, and the choice inside a stretch is which buffer goes last. Fixing the last removal is what makes the two sides independent, because that buffer stands between them until the end and so its neighbours are the stretch's own bounds. Fill by increasing stretch length, record which buffer closed each stretch, and the order falls out of the same table. The final part breaks that model on purpose and the state grows a dimension to answer it.", "dataStructures": ["The widths padded with a sentinel of one at each end, so a boundary buffer is an ordinary buffer.", "A square grid of best savings indexed by the two bounds of a stretch, filled by increasing length.", "A second grid of the same shape recording which buffer closed each stretch, which is the only thing that separates knowing the number from knowing the plan.", "A dictionary keyed on a stretch and a carried run length for the final part, because that key is exactly the state the two-dimensional grid could not hold."], "types": ["A buffer is a struct with an identifier, a width and a stage type, so the last part has something to group on without a parallel array.", "The error enum separates a bad width, a width beyond the supported range, a repeated identifier and a chain that is too long, and the length error carries the length so a caller can see how far over it is.", "The carried run length is wrapped in a small key type for the memo rather than left as a tuple, which makes the third dimension visible in the code rather than implied by it.", "The supported maxima are static constants with doc comments explaining what each one protects, because the enumeration ceiling and the arithmetic ceiling are different numbers for different reasons."], "composition": "The exhaustive baseline is not scaffolding, it is the proof. The suite asserts the fast planner equals it on every chain short enough to reach, which is an assertion that can be red where a call would be green. The order-reporting part composes by calling the same table-building helper the saving uses, so there is one decomposition. The final part composes with nothing, and saying that clearly is the honest answer rather than manufacturing a call.", "tradeoff": "There is no space optimisation here and I want to say so rather than let it look like an omission. An entry reads its own row and its own column at arbitrary distance, so the usual rolling-row trick has nothing to roll. The second grid recording the choices doubles the space and is what buys the plan; without it the number is all you can ever report.", "trap": "The fill order. A plain row-major sweep reads entries that have not been written yet and returns a smaller number that looks entirely reasonable, which is why the baseline exists. The second trap is the last-versus-first reading of the choice inside a stretch: choosing the first removal feels natural and does not decompose, and finding that out after writing the loop is expensive."}, "coding": {"order": ["Write the validation and the replay of one concrete order first, because the baseline, the order-reporting part and my own checking all need the replay.", "Write the exhaustive baseline next and confirm the worked chain reports one hundred and sixty-seven.", "Write the interval table with the padding, and write the loop over stretch lengths before the loop over positions so the fill order is visible.", "Compare the table against the baseline on the worked chain before writing anything else, because everything after this depends on it.", "Add the second grid recording which buffer closed each stretch, then the recursive walk that reads it.", "Replay the reported order through the helper written first and confirm it reproduces the table's number.", "Write the run-collapse part last, starting by saying out loud why the earlier table cannot be reused."], "commentary": ["Padding with a sentinel at each end removes every boundary case, which is worth more than the line it costs.", "An entry is the best saving from retiring everything strictly between these two bounds. That is why the bounds are the neighbours that survive throughout.", "I am asking which buffer goes last, not first. Once the last one is fixed its neighbours are the bounds, and the two sides stop interacting.", "The outer loop is over stretch length, not over rows. An entry depends only on strictly shorter stretches and this is the only order that guarantees they are there.", "There is no rolling-row form of this. An entry reads its own row and its own column at any distance, so the usual space trick does not apply.", "The second grid records which buffer closed each stretch. Without it I can report the number and nothing else, and a caller wants the plan.", "I emit both sides before the closing buffer, because that is what closing last means. Either interleaving of the sides is valid; I am fixing one.", "For the last part the stretch is no longer enough state. Two identical stretches are worth different amounts depending on what is attached to their left, so the count joins the key."], "checkIn": "Before the last part, say: this one breaks the model the first three share, because the reward now depends on history and a stretch alone cannot express that. Do you want the third dimension written out, or would you rather I spent the time on the reconstruction I already have?"}, "verification": {"walkthroughs": ["Walk a chain of one by hand: both neighbours are sentinels, so the saving is that buffer's own width, which is the smallest case the padding has to get right.", "Walk a chain of two with widths three and four: retiring the three first earns one times three times four, then the four is alone and earns four, giving sixteen; retiring the four first gives fifteen. Sixteen is the answer and the order matters already.", "Walk the worked chain of widths three, one, five and eight: the exhaustive baseline reports one hundred and sixty-seven, a row-major fill reports sixty-three, and retiring the narrowest first reports seventy-eight. Three different numbers from three different mistakes.", "Walk the reported order back through the replay: buf-b, then buf-c, then buf-a, then buf-d, summing the neighbour products, and confirm the total lands on the same one hundred and sixty-seven.", "Walk the run-collapse fixture of widths ten, one and ten with the outer two sharing a stage type: retiring each where it stands earns twenty-one, while clearing the middle one first lets the outer two retire as one run of two for forty, giving forty-one."], "tests": "Run swift test --filter Problem31PipelineBufferRemovalPlannerTests and read the four suites in part order. The first suite's two-buffer case is the smallest thing that catches a missing sentinel; the second suite's comparison against the baseline on every short chain is the real correctness evidence; the third suite replays the order rather than trusting it; and the fourth suite compares the quartic planner against an exhaustive search written independently, which is the only honest way to check a recurrence that intricate.", "complexity": "The baseline is factorial in the chain length, which is why it is capped at eight. The interval planner is cubic in time and quadratic in space, and the second grid does not change either. The order walk is linear in the chain. The run-collapse part is quartic in time, because there are a cubic number of states and each considers a linear number of later buffers of the same stage type, and its space is cubic.", "moreTime": "Reporting which buffers were retired together in the run-collapse variant, which needs the same trick as the order reconstruction applied to a three-dimensional record. Handling a chain that is a tree of stages rather than a line, where the stretch becomes a subtree and the last-removal argument still holds. And the space question, which is the honest place to say that this pattern has no rolling-row form rather than pretending to one."}, "code": "/// One intermediate buffer between two pipeline stages.\n///\n/// The width is what the scheduler has to reconcile when the buffer is retired,\n/// and the stage type is what Part 4's run collapse groups on.\npublic struct PipelineBuffer: Equatable, Sendable {\n    public let id: String\n    public let width: Int\n    public let stageType: String\n\n    public init(id: String, width: Int, stageType: String) {\n        self.id = id\n        self.width = width\n        self.stageType = stageType\n    }\n}\n\npublic enum RemovalError: Error, Equatable, Sendable {\n    case nonPositiveWidth(String)\n    case widthTooLarge(String)\n    case duplicateBufferID(String)\n    case chainTooLong(Int)\n    case notImplemented\n}\n\npublic struct BufferRemovalPlanner: Sendable {\n    /// The longest chain the permutation baseline will attempt. Every order of\n    /// retirement is tried, so the work grows as the factorial of the length and\n    /// nine buffers is already a different order of magnitude from eight.\n    public static let maximumExhaustiveChainLength = 8\n\n    /// The longest chain the planners will accept. The run-collapse planner is\n    /// quartic in this number, and the ceiling is also what makes the arithmetic\n    /// provably safe: see `maximumBufferWidth`.\n    public static let maximumPlannableChainLength = 40\n\n    /// The widest buffer accepted. Three widths are multiplied together in a\n    /// single term and the terms are summed once per buffer, so this ceiling\n    /// keeps the largest reachable total far inside an Int. An Int overflow in\n    /// Swift is a trap that ends the process rather than a wrong answer, so the\n    /// bound is checked before any arithmetic runs.\n    public static let maximumBufferWidth = 1_000\n\n    public init() {}\n\n    // MARK: Part 1 - Exhaustive saving for a short chain\n\n    /// The best total saving, found by trying every order of retirement.\n    ///\n    /// Deliberately the slow way. It exists so the interval planner in Part 2\n    /// has something to be proved equal to, and the suite asserts that equality\n    /// on every fixture short enough to reach here. That agreement is the\n    /// strongest correctness evidence an interval decomposition can have.\n    public func bestSavingExhaustive(_ buffers: [PipelineBuffer]) throws(RemovalError) -> Int {\n        let widths = try validatedWidths(buffers, limit: Self.maximumExhaustiveChainLength)\n        guard !widths.isEmpty else { return 0 }\n\n        var order = Array(0..<widths.count)\n        var best = 0\n        permute(&order, from: 0) { candidate in\n            best = max(best, replaySaving(candidate, widths: widths))\n        }\n        return best\n    }\n\n    // MARK: Part 2 - Best saving by interval decomposition\n\n    /// The best total saving, found by decomposing the chain into intervals.\n    ///\n    /// A projection of the table `retirementOrder` also walks, so there is one\n    /// decomposition in this file and both parts read the same numbers out of it.\n    public func bestSaving(_ buffers: [PipelineBuffer]) throws(RemovalError) -> Int {\n        let widths = try validatedWidths(buffers, limit: Self.maximumPlannableChainLength)\n        guard !widths.isEmpty else { return 0 }\n        return savingTable(widths).best[0][widths.count + 1]\n    }\n\n    // MARK: Part 3 - Report the retirement order\n\n    /// The IDs of the buffers in an order that achieves `bestSaving`.\n    ///\n    /// The table records, for every interval, which buffer was retired last\n    /// inside it. Walking that record emits both sides of an interval before the\n    /// buffer that closes it, which is exactly what \"retired last\" means, and\n    /// replaying the result reproduces the saving the table reported.\n    public func retirementOrder(_ buffers: [PipelineBuffer]) throws(RemovalError) -> [String] {\n        let widths = try validatedWidths(buffers, limit: Self.maximumPlannableChainLength)\n        guard !widths.isEmpty else { return [] }\n\n        let table = savingTable(widths)\n        var order: [Int] = []\n        appendOrder(from: 0, to: widths.count + 1, lastRemoved: table.lastRemoved, into: &order)\n        // The table is padded with a sentinel at each end, so a padded index of\n        // one is the first real buffer.\n        return order.map { buffers[$0 - 1].id }\n    }\n\n    // MARK: Part 4 - Collapse runs of one stage type\n\n    /// The best total saving once consecutive buffers of the same stage type may\n    /// be retired together, the whole run reconciled once.\n    ///\n    /// A run of r buffers saves r squared, scaled by the width of the buffer the\n    /// scheduler reconciles the run at, which is the rightmost of them.\n    ///\n    /// Nothing from Part 2 is reused, and that is the lesson rather than an\n    /// oversight: the saving now depends on how many same-type buffers were\n    /// already folded in from the left, so an interval on its own is no longer\n    /// enough state to describe a sub-problem. The state gains a third dimension\n    /// and the two-dimensional table cannot express it.\n    public func bestSavingWithRunCollapse(_ buffers: [PipelineBuffer]) throws(RemovalError) -> Int {\n        let widths = try validatedWidths(buffers, limit: Self.maximumPlannableChainLength)\n        guard !widths.isEmpty else { return 0 }\n\n        let types = buffers.map(\\.stageType)\n        // Keyed on the interval and the carried run length, which is the third\n        // dimension the interval alone could not carry.\n        var memo: [CollapseKey: Int] = [:]\n        return collapseSaving(\n            from: 0, to: widths.count - 1, carried: 0,\n            widths: widths, types: types, memo: &memo\n        )\n    }\n\n    // MARK: Shared machinery\n\n    /// Checks the chain once, for every part, and hands back the widths in order.\n    ///\n    /// Every guard fires before any arithmetic. The width ceiling is not\n    /// decoration: an unchecked multiply here would trap and take the process\n    /// down rather than returning something a caller could inspect.\n    private func validatedWidths(\n        _ buffers: [PipelineBuffer],\n        limit: Int\n    ) throws(RemovalError) -> [Int] {\n        guard buffers.count <= limit else { throw .chainTooLong(buffers.count) }\n\n        var seen: Set<String> = []\n        for buffer in buffers {\n            guard buffer.width > 0 else { throw .nonPositiveWidth(buffer.id) }\n            guard buffer.width <= Self.maximumBufferWidth else { throw .widthTooLarge(buffer.id) }\n            guard seen.insert(buffer.id).inserted else { throw .duplicateBufferID(buffer.id) }\n        }\n        return buffers.map(\\.width)\n    }\n\n    /// Every order of retirement, handed one at a time to `body`.\n    private func permute(_ order: inout [Int], from start: Int, _ body: ([Int]) -> Void) {\n        if start == order.count {\n            body(order)\n            return\n        }\n        for position in start..<order.count {\n            order.swapAt(start, position)\n            permute(&order, from: start + 1, body)\n            order.swapAt(start, position)\n        }\n    }\n\n    /// What one concrete order of retirement actually saves.\n    ///\n    /// A retired buffer's saving is its own width times the widths of the\n    /// buffers still present on either side of it, with a sentinel width of one\n    /// standing in at each end of the chain. Shared by the baseline and by the\n    /// suite's replay of the reported order.\n    private func replaySaving(_ order: [Int], widths: [Int]) -> Int {\n        var remaining = Array(0..<widths.count)\n        var total = 0\n        for index in order {\n            guard let position = remaining.firstIndex(of: index) else { continue }\n            let leftWidth = position > 0 ? widths[remaining[position - 1]] : 1\n            let rightWidth = position < remaining.count - 1 ? widths[remaining[position + 1]] : 1\n            total += leftWidth * widths[index] * rightWidth\n            remaining.remove(at: position)\n        }\n        return total\n    }\n\n    /// The interval table, plus the record of which buffer closes each interval.\n    ///\n    /// The widths are padded with a sentinel of one at each end so the two ends\n    /// of the chain need no special case at all. An entry spans the buffers\n    /// strictly between its two padded bounds, and it asks which of them is\n    /// retired **last**: once that buffer is fixed, its neighbours at retirement\n    /// time are the interval's own bounds, and the two sides stop interacting.\n    ///\n    /// Filled by increasing interval length, which is the whole fill order.\n    /// A plain row-major sweep reads entries that have not been written yet and\n    /// returns a plausible, smaller number rather than failing.\n    private func savingTable(_ widths: [Int]) -> (best: [[Int]], lastRemoved: [[Int]]) {\n        let padded = [1] + widths + [1]\n        let span = padded.count\n        var best = Array(repeating: Array(repeating: 0, count: span), count: span)\n        var lastRemoved = Array(repeating: Array(repeating: -1, count: span), count: span)\n\n        guard span >= 3 else { return (best, lastRemoved) }\n        for length in 2..<span {\n            for left in 0...(span - 1 - length) {\n                let right = left + length\n                for closing in (left + 1)..<right {\n                    let candidate = best[left][closing]\n                        + padded[left] * padded[closing] * padded[right]\n                        + best[closing][right]\n                    if candidate > best[left][right] || lastRemoved[left][right] == -1 {\n                        best[left][right] = candidate\n                        lastRemoved[left][right] = closing\n                    }\n                }\n            }\n        }\n        return (best, lastRemoved)\n    }\n\n    /// Walks the record of closing buffers, emitting both sides of an interval\n    /// before the buffer that closes it.\n    private func appendOrder(\n        from left: Int,\n        to right: Int,\n        lastRemoved: [[Int]],\n        into order: inout [Int]\n    ) {\n        guard right - left >= 2 else { return }\n        let closing = lastRemoved[left][right]\n        guard closing >= 0 else { return }\n        appendOrder(from: left, to: closing, lastRemoved: lastRemoved, into: &order)\n        appendOrder(from: closing, to: right, lastRemoved: lastRemoved, into: &order)\n        order.append(closing)\n    }\n\n    /// The third dimension, made a type so the memo reads as what it is.\n    private struct CollapseKey: Hashable {\n        let from: Int\n        let to: Int\n        let carried: Int\n    }\n\n    /// Best saving for the buffers `from...to`, given that `carried` buffers of\n    /// the same stage type as the one at `from` are already folded in from the\n    /// left and will be retired in the same run. The carried buffers sit to the\n    /// left of `from` and share its stage type.\n    ///\n    /// Two moves, and the second one is the reason this needs its own table:\n    /// close the run here, so it saves the square of its length scaled by this\n    /// buffer's width; or defer this buffer, clear everything between it and a\n    /// later buffer of the same stage type, and let the run grow.\n    private func collapseSaving(\n        from: Int,\n        to: Int,\n        carried: Int,\n        widths: [Int],\n        types: [String],\n        memo: inout [CollapseKey: Int]\n    ) -> Int {\n        guard from <= to else { return 0 }\n        let key = CollapseKey(from: from, to: to, carried: carried)\n        if let cached = memo[key] { return cached }\n\n        // Close the run at this buffer. Its length is what was carried in plus\n        // this one, and the width that scales it is this buffer's, because this\n        // is where the scheduler reconciles the run.\n        var best = (carried + 1) * (carried + 1) * widths[from]\n            + collapseSaving(\n                from: from + 1, to: to, carried: 0,\n                widths: widths, types: types, memo: &memo\n            )\n\n        // Or defer it. Everything strictly between here and the buffer it joins\n        // has to go first, which is what makes the two sides independent.\n        if from + 1 <= to {\n            for later in (from + 1)...to where types[later] == types[from] {\n                let deferred = collapseSaving(\n                    from: from + 1, to: later - 1, carried: 0,\n                    widths: widths, types: types, memo: &memo\n                ) + collapseSaving(\n                    from: later, to: to, carried: carried + 1,\n                    widths: widths, types: types, memo: &memo\n                )\n                best = max(best, deferred)\n            }\n        }\n\n        memo[key] = best\n        return best\n    }\n}\n"};
