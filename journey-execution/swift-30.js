// Generated by tools/build_journey_data.py; do not edit.
window.INTERVIEW_EXECUTION = window.INTERVIEW_EXECUTION || {};
window.INTERVIEW_EXECUTION["swift-30"] = {"timeBudget": {"navigation": 4, "solution": 9, "coding": 26, "verification": 6}, "navigation": {"say": "All four of these are one table over a pair of prefixes, and the only thing that changes between them is what the entry means and what a move costs. I want to say that first, because it decides how I lay the code out before I write any of it.", "questions": ["Are clause identifiers compared exactly, or is there a notion of a clause being edited but still the same clause?", "When two scripts are equally short, does the caller care which one comes back, or only that it is the same one every time?", "Can a review weight be zero, meaning a clause a reviewer will not look at, or is one the floor?"], "assumptions": ["Clause identifiers compare exactly; two clauses are the same clause or they are not.", "Ties are broken by a rule I state and document, because a caller asserting one specific answer needs it to be reproducible rather than optimal in some further sense.", "A weight of zero is allowed and means a clause nobody re-reads; a negative weight is a refusal.", "A clause the caller did not price reviews like an ordinary one, so it costs one."], "edgeCases": ["either revision empty, which is the base case every part stands on", "two revisions holding the same clauses in a different order, where several answers cost the same", "a revision with a clause repeated, so the walk cannot assume identifiers are unique", "a clause priced far above the rest, which is what makes the weighted answer differ in shape rather than only in size"], "restate": "Given two ordered lists of clause identifiers, report what they still agree on, the smallest document containing both, the cheapest script of edits from one to the other, and that same script priced by per-clause review weight."}, "solution": {"pitch": "One table over pairs of prefixes, filled ascending, four times over. The first objective maximises agreement; the third minimises edits and differs in exactly one place, its base rows. The second and fourth are not new tables at all: one walks the first table backwards to recover the clauses, and the other hands the third table a cost function instead of a constant.", "dataStructures": ["A two-dimensional array of integers whose rows and columns are one longer than the revisions, so the empty prefix has a row and a column of its own rather than a special case.", "The whole table kept rather than two rolling rows, because the walk back out of it is what produces the clauses and the script.", "A dictionary from clause identifier to weight, built once at the top of the weighted call so the table itself never re-scans the weight list.", "The script accumulated backwards and reversed once, rather than prepended to inside the loop."], "types": ["An edit is an enum with associated values, so a delete carrying two identifiers or a replacement carrying one cannot be constructed at all.", "The script and its cost travel together in one value, because they always come out of the same walk of the same table and returning them separately invites them to disagree.", "The cost model is its own small type with three named operations and a case where everything costs one, which is what makes the unweighted part a call rather than a copy.", "The error enum separates a negative weight from a clause priced twice, and both carry the clause identifier, because those are two different things for a caller to fix."], "composition": "Every link here is a call rather than a description. The length is the agreement's count. The distance is the script's cost. The unweighted script is the weighted one with no weights supplied. And the delete-only distance is the two lengths minus twice the agreement, which is this problem proving its third part against its first: the suite asserts that identity on a spread of inputs, and it can be red where a call would be green.", "tradeoff": "Two rolling rows would cut the space from quadratic to linear, and I am deliberately not doing it, because the walk back out of the table is what produces the clauses and the script, and a collapsed table cannot be walked. If the revisions were large enough for that to matter the answer is a divide-and-conquer alignment that recovers the path in linear space at the same time cost, which is worth naming and not worth writing in this hour.", "trap": "The base rows. The two objectives share a table shape and differ in exactly one place, so pasting the first one's empty edges into the second reports distances that are far too small and look plausible. The second trap is tie-breaking: for most inputs several answers are equally good, and an unstated rule makes every specific assertion about the output unreliable."}, "coding": {"order": ["Write the agreement table first, with the entry's meaning stated in a comment above it.", "Check it on two identical revisions and on one empty revision before writing anything that reads it.", "Write the walk back out of it next, and state the tie-break as you write the comparison rather than afterwards.", "Write the merge as the same walk with the stepped-over clause emitted, then drain whichever side is left over.", "Write the alignment table for the edit objective, and write its base rows before its loop, because that is where the difference lives.", "Pull the cost out into a model before writing the walk that produces the script, so the weighted part is a parameter rather than a rewrite.", "Finish with the validation of the weights, which is small and belongs at the top of the one call that needs it."], "commentary": ["One entry means the answer for this many clauses of the left revision and this many of the right. Everything else follows from that, so it goes in a comment before the loop.", "The zero row and the zero column are the empty prefix, which is why the table is one bigger than the input in both directions.", "I am reading the clause before the index, not at it, because row three is about the first three clauses and that is the third one.", "On a tie I step up, dropping from the left. I am stating that now because a caller asserting one specific answer needs it to be the same every time.", "This second table's edges are not empty. Turning a prefix into nothing means deleting all of it, and that one line is the difference between the two objectives.", "The precedence for the walk is keep, then replace, then delete, then insert. Any other order is equally correct and produces a different script, which is exactly why I am fixing one.", "I am not building a table for the delete-only variant. Every clause outside the agreement has to move, and I already know how large the agreement is.", "The cost model comes out first, with a case where everything costs one. That makes the unweighted part a call into this machinery rather than a second copy of it."], "checkIn": "Before the third part, say: this is the same table with a minimum instead of a maximum, and the only real change is the base rows. Shall I write it that way, or would you rather see me derive the second table from scratch to show I know why the edges differ?"}, "verification": {"walkthroughs": ["Walk the four-by-four table for the worked revisions by hand: recitals matches at once, payment-terms has no partner, liability and notices both match, and the corner reads three.", "Walk back from that corner: notices matches and is emitted, then the walk steps past arbitration and payment-terms, then liability and recitals match, giving the agreement in three clauses once reversed.", "Check the merge by arithmetic rather than by eye: four plus four minus three is five, and reading the merged list confirms both revisions still appear in order inside it.", "Walk the delete-only identity on the same pair: four plus four minus twice three is two, which is what the delete-only method must report, and it is derived without touching the second table at all.", "Price payment-terms at nine and walk the alternatives: replacing it costs nine while deleting it and inserting elsewhere costs ten, and the total lands on ten either way, which is why the second fixture with the swapped clauses is the one that proves the weights change the shape."], "tests": "Run swift test --filter Problem30DocumentRevisionDiffEngineTests and read the four suites in part order. The greedy fixture in the first suite catches taking the first match; the empty-in-both-directions test in the third suite catches the pasted base rows; the test that runs the script against the left revision and compares it with the right is the strongest single assertion in the problem; and the everything-costs-one test in the fourth suite catches a second table.", "complexity": "Every part is the product of the two lengths in time and in space, and the walk back out of the table is linear in the two lengths added. The weighted part adds a dictionary lookup per entry, which does not change the order. The delete-only variant is the one exception: it is the agreement table's cost and nothing more, because it is arithmetic on a number that part already computed.", "moreTime": "Recovering the alignment in linear space by recursively bisecting the longer revision and meeting a forward and a backward pass in the middle, which keeps the same time cost. Adding a transposition move for two clauses that swapped places, which needs a guard two rows and two columns back. Aligning three revisions at once, which is the same shape in three dimensions and is where the cost stops being acceptable."}, "code": "/// One clause of an agreement, together with what reviewing a change to it\n/// costs. The weight is a reviewer-minutes proxy: replacing a payment-terms\n/// clause costs a lawyer far more attention than replacing a boilerplate\n/// notice, and Part 4 exists because that difference changes which alignment\n/// is actually the cheapest one.\npublic struct Clause: Hashable, Sendable {\n    public let id: String\n    public let reviewWeight: Int\n\n    public init(id: String, reviewWeight: Int) {\n        self.id = id\n        self.reviewWeight = reviewWeight\n    }\n}\n\n/// One step of a diff. An enum with associated values rather than a struct with\n/// a kind field, so an operation that carries the wrong number of clause IDs\n/// cannot be constructed at all.\npublic enum EditOperation: Equatable, Sendable {\n    case keep(String)\n    case delete(String)\n    case insert(String)\n    case replace(from: String, to: String)\n}\n\n/// A diff plus what it costs to review. The cost travels with the operations\n/// because the two are always computed from the same walk of the same table,\n/// and returning them separately invites them to disagree.\npublic struct EditScript: Equatable, Sendable {\n    public let operations: [EditOperation]\n    public let totalCost: Int\n\n    public init(operations: [EditOperation], totalCost: Int) {\n        self.operations = operations\n        self.totalCost = totalCost\n    }\n}\n\npublic enum DiffError: Error, Equatable, Sendable {\n    case negativeReviewWeight(String)\n    case duplicateClauseWeight(String)\n    case notImplemented\n}\n\npublic struct RevisionDiffEngine: Sendable {\n    public init() {}\n\n    // MARK: Part 1 - Measure the shared spine\n\n    /// How many clauses survive, in order, from one revision into the other.\n    ///\n    /// This is the length of the alignment table's corner entry. It is written\n    /// as a projection of `sharedSpine` rather than as its own table, because\n    /// two tables computing the same quantity is exactly how the two answers\n    /// drift apart.\n    public func sharedSpineLength(_ left: [String], _ right: [String]) -> Int {\n        sharedSpine(left, right).count\n    }\n\n    // MARK: Part 2 - Emit the spine and the merged revision\n\n    /// The clauses common to both revisions, in the order both agree on.\n    ///\n    /// The table entry at row i and column j is the length of the shared spine\n    /// of the first i clauses on the left and the first j on the right, so the\n    /// zero row and the zero column are both empty: an empty revision shares\n    /// nothing with anything. The walk back from the corner is where the spine\n    /// itself comes from, and it is the reason the whole table is kept rather\n    /// than the two rolling rows the length alone would need.\n    ///\n    /// On a tie the walk steps **up**, dropping a clause from the left. The\n    /// rule is documented rather than incidental because a caller that asserts\n    /// one particular spine needs the answer to be the same one every time.\n    public func sharedSpine(_ left: [String], _ right: [String]) -> [String] {\n        let table = spineTable(left, right)\n        var spine: [String] = []\n        var i = left.count\n        var j = right.count\n\n        while i > 0 && j > 0 {\n            if left[i - 1] == right[j - 1] {\n                spine.append(left[i - 1])\n                i -= 1\n                j -= 1\n            } else if table[i - 1][j] >= table[i][j - 1] {\n                i -= 1\n            } else {\n                j -= 1\n            }\n        }\n\n        return Array(spine.reversed())\n    }\n\n    /// The shortest document that contains both revisions as subsequences.\n    ///\n    /// The same walk as `sharedSpine`, except that the clause stepped over is\n    /// emitted rather than discarded. Its length is necessarily the two lengths\n    /// added together with the shared spine counted once instead of twice, and\n    /// the test suite asserts exactly that against Part 1.\n    public func mergedRevision(_ left: [String], _ right: [String]) -> [String] {\n        let table = spineTable(left, right)\n        var merged: [String] = []\n        var i = left.count\n        var j = right.count\n\n        while i > 0 && j > 0 {\n            if left[i - 1] == right[j - 1] {\n                merged.append(left[i - 1])\n                i -= 1\n                j -= 1\n            } else if table[i - 1][j] >= table[i][j - 1] {\n                merged.append(left[i - 1])\n                i -= 1\n            } else {\n                merged.append(right[j - 1])\n                j -= 1\n            }\n        }\n        while i > 0 {\n            merged.append(left[i - 1])\n            i -= 1\n        }\n        while j > 0 {\n            merged.append(right[j - 1])\n            j -= 1\n        }\n\n        return Array(merged.reversed())\n    }\n\n    // MARK: Part 3 - Minimal edit script\n\n    /// The fewest single-clause edits that turn the left revision into the right.\n    ///\n    /// A projection of `editScript`, for the same reason `sharedSpineLength` is\n    /// a projection of `sharedSpine`: there is one alignment table in this file\n    /// and every number in this part comes out of it.\n    public func editDistance(_ left: [String], _ right: [String]) -> Int {\n        editScript(left, right).totalCost\n    }\n\n    /// The fewest edits when replacement is forbidden and only insertion and\n    /// deletion are available.\n    ///\n    /// Nothing new is computed. Every clause outside the shared spine has to be\n    /// deleted from the left or inserted into the right, so the answer is the\n    /// two lengths added together with the spine removed from each of them.\n    /// This is Part 3 proving itself against Part 1, and the suite asserts the\n    /// identity directly on a spread of fixtures.\n    public func deleteOnlyDistance(_ left: [String], _ right: [String]) -> Int {\n        left.count + right.count - 2 * sharedSpineLength(left, right)\n    }\n\n    /// The cheapest script of keeps, deletes, inserts and replacements that\n    /// turns the left revision into the right, under unit review cost.\n    ///\n    /// The unit cost model is the weighted model of Part 4 with no weights\n    /// supplied, so this method is a call into the shared machinery rather than\n    /// a second implementation of it.\n    public func editScript(_ left: [String], _ right: [String]) -> EditScript {\n        alignedScript(left, right, cost: .unit)\n    }\n\n    // MARK: Part 4 - Price the script with review weights\n\n    /// The cheapest script under per-clause review weights.\n    ///\n    /// Same table, same walk, different cost function. A clause with no listed\n    /// weight reviews like any other, so it costs one; the default lives in the\n    /// cost model rather than being spread through the table.\n    public func weightedEditScript(\n        _ left: [String],\n        _ right: [String],\n        weights: [Clause]\n    ) throws(DiffError) -> EditScript {\n        var byID: [String: Int] = [:]\n        for clause in weights {\n            guard clause.reviewWeight >= 0 else { throw .negativeReviewWeight(clause.id) }\n            guard byID[clause.id] == nil else { throw .duplicateClauseWeight(clause.id) }\n            byID[clause.id] = clause.reviewWeight\n        }\n        return alignedScript(left, right, cost: ReviewCostModel(weights: byID))\n    }\n\n    // MARK: Shared machinery\n\n    /// What one edit costs the reviewer.\n    ///\n    /// A replacement is priced by the more sensitive of the two clauses\n    /// involved, because the review time is set by whichever side needs the\n    /// closer reading. With no weights listed every edit costs one, which is\n    /// why Part 3 is this model rather than a separate table.\n    private struct ReviewCostModel: Sendable {\n        let weights: [String: Int]\n\n        static let unit = ReviewCostModel(weights: [:])\n\n        func removal(_ id: String) -> Int { weights[id] ?? 1 }\n        func addition(_ id: String) -> Int { weights[id] ?? 1 }\n        func substitution(from: String, to: String) -> Int {\n            max(removal(from), addition(to))\n        }\n    }\n\n    /// The shared-spine table. Entry i, j is the length of the shared spine of\n    /// the first i clauses on the left and the first j on the right.\n    ///\n    /// Filled ascending in both dimensions, which is what makes the three\n    /// entries every transition reads - above, to the left, and diagonally\n    /// above-left - already present when the transition needs them.\n    private func spineTable(_ left: [String], _ right: [String]) -> [[Int]] {\n        var table = Array(\n            repeating: Array(repeating: 0, count: right.count + 1),\n            count: left.count + 1\n        )\n        guard !left.isEmpty && !right.isEmpty else { return table }\n\n        for i in 1...left.count {\n            for j in 1...right.count {\n                if left[i - 1] == right[j - 1] {\n                    table[i][j] = table[i - 1][j - 1] + 1\n                } else {\n                    table[i][j] = max(table[i - 1][j], table[i][j - 1])\n                }\n            }\n        }\n        return table\n    }\n\n    /// The alignment table for a given cost model. Entry i, j is the cheapest\n    /// review cost that turns the first i clauses on the left into the first j\n    /// on the right.\n    ///\n    /// The zero row and the zero column are **not** zero here, and that single\n    /// difference from the spine table is what separates the two objectives:\n    /// turning a prefix into nothing means deleting all of it, and turning\n    /// nothing into a prefix means inserting all of it.\n    private func alignmentTable(\n        _ left: [String],\n        _ right: [String],\n        cost: ReviewCostModel\n    ) -> [[Int]] {\n        var table = Array(\n            repeating: Array(repeating: 0, count: right.count + 1),\n            count: left.count + 1\n        )\n        if !left.isEmpty {\n            for i in 1...left.count {\n                table[i][0] = table[i - 1][0] + cost.removal(left[i - 1])\n            }\n        }\n        if !right.isEmpty {\n            for j in 1...right.count {\n                table[0][j] = table[0][j - 1] + cost.addition(right[j - 1])\n            }\n        }\n        guard !left.isEmpty && !right.isEmpty else { return table }\n\n        for i in 1...left.count {\n            for j in 1...right.count {\n                let deleting = table[i - 1][j] + cost.removal(left[i - 1])\n                let inserting = table[i][j - 1] + cost.addition(right[j - 1])\n                let diagonal: Int\n                if left[i - 1] == right[j - 1] {\n                    diagonal = table[i - 1][j - 1]\n                } else {\n                    diagonal = table[i - 1][j - 1]\n                        + cost.substitution(from: left[i - 1], to: right[j - 1])\n                }\n                table[i][j] = min(diagonal, min(deleting, inserting))\n            }\n        }\n        return table\n    }\n\n    /// The cheapest script under a cost model, walked back out of the table.\n    ///\n    /// The walk has to break ties, and the rule is fixed and documented: keep\n    /// first where the clauses match, then replace, then delete, then insert.\n    /// Any other rule is equally correct arithmetically and produces a\n    /// different script, which is precisely why the caller is told which one\n    /// this is.\n    private func alignedScript(\n        _ left: [String],\n        _ right: [String],\n        cost: ReviewCostModel\n    ) -> EditScript {\n        let table = alignmentTable(left, right, cost: cost)\n        var operations: [EditOperation] = []\n        var i = left.count\n        var j = right.count\n\n        while i > 0 || j > 0 {\n            if i > 0 && j > 0 && left[i - 1] == right[j - 1]\n                && table[i][j] == table[i - 1][j - 1] {\n                operations.append(.keep(left[i - 1]))\n                i -= 1\n                j -= 1\n            } else if i > 0 && j > 0\n                && table[i][j] == table[i - 1][j - 1]\n                    + cost.substitution(from: left[i - 1], to: right[j - 1]) {\n                operations.append(.replace(from: left[i - 1], to: right[j - 1]))\n                i -= 1\n                j -= 1\n            } else if i > 0 && table[i][j] == table[i - 1][j] + cost.removal(left[i - 1]) {\n                operations.append(.delete(left[i - 1]))\n                i -= 1\n            } else {\n                operations.append(.insert(right[j - 1]))\n                j -= 1\n            }\n        }\n\n        return EditScript(\n            operations: Array(operations.reversed()),\n            totalCost: table[left.count][right.count]\n        )\n    }\n}\n"};
