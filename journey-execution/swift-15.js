// Generated by tools/build_journey_data.py; do not edit.
window.INTERVIEW_EXECUTION = window.INTERVIEW_EXECUTION || {};
window.INTERVIEW_EXECUTION["swift-15"] = {"timeBudget": {"navigation": 5, "solution": 10, "coding": 24, "verification": 6}, "navigation": {"say": "I’ll confirm the behavioral contract for Tic-Tac-Toe Engine (Swift) before choosing storage.", "questions": ["Can I confirm bounds, configurable win runs, draws, and generic players?", "Which ordering guarantees are observable at the API boundary?"], "assumptions": ["The supplied domain types and typed failures are the public contract.", "Injected timestamps are authoritative and equality follows the stated boundary."], "edgeCases": ["bounds", "configurable win runs", "draws", "and generic players"], "restate": "Build a generic value-semantic board with typed outcomes, independent copies, and configurable winning runs. Later parts will compose earlier behavior."}, "solution": {"pitch": "Value-semantic state uses dictionaries for identity, sets for uniqueness, and sorting only at deterministic output boundaries.", "dataStructures": ["Dictionaries provide identity lookup; sets enforce uniqueness.", "Arrays preserve audit/event/history order; derived views are computed by shared helpers."], "types": ["Domain structs and exhaustive enums prevent invalid stringly-typed states.", "Rich Result/error/outcome values preserve the reason later parts need."], "composition": "Part 1: How can one directional scan express rows, columns, and both diagonal families? → Part 2: Which operation should be the single source of truth for the result after a move? → Part 3: How does winLength change a full-line check into a consecutive-run check?", "tradeoff": "Prefer clear invariants and deterministic scans; add indexes behind the interface only after measuring scale.", "trap": "Spot the composition trap early: How can one directional scan express rows, columns, and both diagonal families? must return or preserve enough evidence for How does winLength change a full-line check into a consecutive-run check?; otherwise later code would duplicate the decision."}, "coding": {"order": ["Part 1: Represent occupied cells by Position.", "Part 2: Validate the position before changing storage.", "Part 3: Validate size and win length together."], "commentary": ["I’m establishing the canonical lookup/reduction first.", "Every mutation validates before committing, and later APIs delegate to the earlier boundary.", "Copies remain independent because mutable state is stored on the value instance."], "checkIn": "Pause after Part 1 tests: ‘Does this result preserve the evidence we need for the later parts?’"}, "verification": {"walkthroughs": ["Walk the stub example and name every state change.", "Walk the boundary cases: bounds, configurable win runs, draws, and generic players.", "Mutate one copy/instance and prove a second remains unchanged."], "tests": "Run the Swift suite for swift/Tests/Problem15TicTacToeEngineTests/Problem15TicTacToeEngineTests.swift and map each ordered suite to its part.", "complexity": "State lookup is O(1) average; derived scans are O(n), with O(n log n) only where deterministic sorting is required.", "moreTime": "Add property-based invariant tests, measured indexes, persistence, and observability without changing the public contract."}, "code": "public enum Mark: Hashable, Sendable { case x, o, custom(String) }\npublic struct Position: Hashable, Sendable {\n    public let row: Int; public let column: Int\n    public init(row: Int, column: Int) { self.row = row; self.column = column }\n}\npublic enum GameResult<Player: Hashable & Sendable>: Equatable, Sendable {\n    case inProgress, won(Player), draw\n}\npublic enum BoardError: Error, Equatable, Sendable {\n    case invalidConfiguration\n    case outOfBounds(Position)\n    case occupied(Position)\n    case notImplemented\n}\n\npublic struct Board<Player: Hashable & Sendable>: Sendable {\n    public let size: Int\n    public let winLength: Int\n    private var cells: [Position: Player]\n\n    public init(size: Int = 3, winLength: Int? = nil, positions: [Position: Player] = [:]) throws(BoardError) {\n        let target = winLength ?? size\n        guard size > 0, target > 0, target <= size else { throw .invalidConfiguration }\n        guard positions.keys.allSatisfy({ (0..<size).contains($0.row) && (0..<size).contains($0.column) }) else { throw .invalidConfiguration }\n        self.size = size; self.winLength = target; self.cells = positions\n    }\n\n    // MARK: Part 1 — generic board analysis\n\n    public func mark(at position: Position) -> Player? { cells[position] }\n\n    public func result() -> GameResult<Player> {\n        for row in 0..<size {\n            for column in 0..<size {\n                if let winner = winner(startingAt: Position(row: row, column: column)) { return .won(winner) }\n            }\n        }\n        return cells.count == size * size ? .draw : .inProgress\n    }\n\n    /// The four step vectors cover every line once: east, south, south-east,\n    /// and south-west. Scanning from every occupied cell reaches all of them.\n    private static var directions: [(rowStep: Int, columnStep: Int)] {\n        [(0, 1), (1, 0), (1, 1), (1, -1)]\n    }\n\n    private func winner(startingAt position: Position) -> Player? {\n        guard let player = cells[position] else { return nil }\n        for direction in Self.directions {\n            let run = (0..<winLength).allSatisfy { offset in\n                cells[Position(row: position.row + direction.rowStep * offset,\n                               column: position.column + direction.columnStep * offset)] == player\n            }\n            if run { return player }\n        }\n        return nil\n    }\n\n    // MARK: Part 2 — mutating moves and value semantics\n\n    public mutating func place(_ player: Player, at position: Position) throws(BoardError) -> GameResult<Player> {\n        guard (0..<size).contains(position.row), (0..<size).contains(position.column) else {\n            throw .outOfBounds(position)\n        }\n        guard cells[position] == nil else { throw .occupied(position) }\n        cells[position] = player\n        // A move's outcome is just the board's analysis after the move.\n        return result()\n    }\n\n    public mutating func reset() { cells.removeAll() }\n}"};
